# VS Code source

The generic renderer combines the project descriptor's `editor` section with task workspace
metadata. This directory is reserved for future project-owned snippets that do not belong in the
generic renderer.
